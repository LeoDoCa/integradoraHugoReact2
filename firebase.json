{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "storage": {
    "rules": "storage.rules",
    "cors": [
      {
        "origin": ["*"],
        "method": ["GET", "HEAD", "POST", "PUT", "DELETE"],
        "maxAgeSeconds": 3600
      }
    ]
  }
}